package com.costumi.backend.identidad.dominio;

/** Estado de una membresía persona↔tienda (Fase B): activa (puede operar) o suspendida (desvinculada). */
public enum EstadoMembresia {
	ACTIVA,
	SUSPENDIDA
}
