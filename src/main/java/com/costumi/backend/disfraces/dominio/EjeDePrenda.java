package com.costumi.backend.disfraces.dominio;

/** Eje de prenda de un slot (RF-2.3): fija (siempre la misma) o personalizable (el cliente elige del pool). */
public enum EjeDePrenda {
	FIJA,
	PERSONALIZABLE
}
